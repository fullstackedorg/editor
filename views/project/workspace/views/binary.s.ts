import style from "style";
import spacing from "@fullstacked/ui/values/spacing.s";

export const binaryViewClass = style.createClass("binary-container", {
    textAlign: "center",
    padding: spacing.m
});
